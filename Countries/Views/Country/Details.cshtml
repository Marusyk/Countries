@model Countries.Models.CountryInfo

@{
    ViewBag.Title = string.Format("Details of {0}", Model.Name);
}

<h1>@ViewBag.Title</h1>

<div class="wrapper">
    <hr/>
        @Html.DisplayForModel()
</div>
<p class="link">
    @Html.ActionLink("Edit", "Edit", new { guid = Model.Guid }) |
    @Html.ActionLink("Back to List", "Index")
</p>
